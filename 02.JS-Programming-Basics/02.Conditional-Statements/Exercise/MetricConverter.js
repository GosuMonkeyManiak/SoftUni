function solve(number, inPut, outPut) {

    number = Number(number);

    if (inPut == "m") {
        
        if (outPut == "cm") {
            
        } else { //mm

        }
    }

    console.log()
}

solve();