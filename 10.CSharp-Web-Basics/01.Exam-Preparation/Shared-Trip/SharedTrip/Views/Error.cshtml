<section class="py-5">
    <div class="container">
        Your request is not valid. Errors:

        <ul>
            @foreach (var error in Model)
            {
                <li>@error</li>
            }
        </ul>
    </div>
</section>